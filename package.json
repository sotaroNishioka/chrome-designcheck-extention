{
  "name": "design-checker-extension",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "watch": "tsc && vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.2",
    "@types/chrome": "^0.0.260",
    "@crxjs/vite-plugin": "^2.0.0-beta.23"
  }
}